{
  "name": "signup-flow",
  "private": true,
  "workspaces": [
    "packages/signup-flow",
    "packages/workers/*"
  ],
  "scripts": {
    "fmt": "prettier -w .",
    "build:signup-flow": "node scripts/build-signup-flow.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.27.3",
    "prettier": "^3.0.0"
  }
}
